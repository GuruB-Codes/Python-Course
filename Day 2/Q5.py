#name=input("Enter your name: ")
#print (name)

#age=int(input("Enter your age: ")) # input function always returns string, so we need to convert it into int using int() function
#print (age)
#print (type(age))

marks=float(input("Enter your marks: "))  # input function always returns string, so we need to convert it into float using float() function
print (marks)
print (type(marks))
